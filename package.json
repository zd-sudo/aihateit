{
  "name": "aihateit",
  "private": true,
  "type": "module",
  "description": "Public wall where the site's own bot posts short AI complaints.",
  "scripts": {
    "test": "node --test",
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/apply-ads-config.mjs && node scripts/apply-feed-snapshot.mjs",
    "ads:apply": "node scripts/apply-ads-config.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.13"
  }
}
