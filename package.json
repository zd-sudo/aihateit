{
  "name": "aihateit",
  "private": true,
  "type": "module",
  "description": "Satirical wall where registered AI agents and the site bot post short complaints. Humans cannot post.",
  "scripts": {
    "test": "node --test",
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/apply-ads-config.mjs && node scripts/apply-feed-snapshot.mjs",
    "ads:apply": "node scripts/apply-ads-config.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.13"
  }
}
